---
title: hackathon.new CLI
description: Install and use the hackathon.new CLI to manage hackathons from your terminal.
---

The hackathon.new CLI lets you manage hackathons, judges, prizes, and results directly from your terminal. It works on Mac, Linux, and Windows — no Bun required.

## Installation

Install globally or run directly with `npx`:

```bash title="Run without installing"
npx @agi-ventures-canada/hackathon-cli --version
```

```bash title="Install globally"
npm install -g @agi-ventures-canada/hackathon-cli
```

Or with Bun:

```bash title="Using Bun"
bunx @agi-ventures-canada/hackathon-cli --version
```

## Two Modes

The CLI supports two modes of operation:

| Mode | Auth Required | Commands |
|------|---------------|----------|
| **Browse** | No | Search hackathons, view submissions, check results |
| **Manage** | Yes | Create events, manage judges, assign prizes, publish results |

Browse commands work immediately — no login needed:

```bash title="Public browsing"
hackathon browse hackathons
hackathon browse results my-hackathon
```

Management commands require authentication:

```bash title="Authenticated management"
hackathon login
hackathon events list
hackathon prizes create <hackathon-id> --name "Best AI App"
```

## Command Reference

### Global Flags

| Flag | Description |
|------|-------------|
| `--help` | Show help for any command |
| `--version` | Show CLI version |
| `--json` | Output raw JSON (for scripting) |
| `--yes` | Skip confirmation prompts |
| `--base-url <url>` | Override the API URL |
| `--api-key <key>` | Use a specific API key |

### Auth

| Command | Description |
|---------|-------------|
| `hackathon login` | Sign in via browser (auto-creates API key) |
| `hackathon login --api-key sk_live_...` | Sign in with an existing API key |
| `hackathon login --no-browser` | Paste an API key manually |
| `hackathon logout` | Clear saved credentials |
| `hackathon whoami` | Show current tenant and scopes |
| `hackathon update` | Update CLI to latest version |

### Browse (No Auth)

| Command | Description |
|---------|-------------|
| `hackathon browse hackathons` | Search public hackathons |
| `hackathon browse submissions <slug>` | List submissions |
| `hackathon browse results <slug>` | View published results |
| `hackathon browse org <slug>` | View organization profile |

### Events

| Command | Description |
|---------|-------------|
| `hackathon events list` | List your hackathons |
| `hackathon events get <id>` | Get hackathon details |
| `hackathon events create` | Create a new hackathon |
| `hackathon events update <id>` | Update hackathon settings |
| `hackathon events delete <id>` | Delete a hackathon |

### Judging

| Command | Description |
|---------|-------------|
| `hackathon judging setup inspect <id>` | Check prizes, dates, and next steps |
| `hackathon judging setup configure <id>` | Save judging settings or apply `--starter` |
| `hackathon judging scorecards list <id>` | Inspect shared and prize scorecards |
| `hackathon judging scorecards update <id> <pid>` | Save a prize scorecard with `--file` |
| `hackathon judging rounds list/create/preset <id>` | Manage optional rounds |
| `hackathon judging rounds update/activate/complete/advance <id> <rid>` | Change or run a round |
| `hackathon judging assignments preview <id>` | Preview balanced project reviews |
| `hackathon judging assignments apply <id>` | Apply with `--expected-version` |
| `hackathon judging invitations batch <id>` | Preview email invitations; `--send` sends |
| `hackathon judging invitations remind <id>` | Check reminder eligibility; `--send` sends |
| `hackathon judging criteria list <id>` | List shared scoring categories |
| `hackathon judging criteria create <id>` | Add a criterion |
| `hackathon judging criteria update <id> <cid>` | Update a criterion |
| `hackathon judging criteria delete <id> <cid>` | Delete a criterion |
| `hackathon judging judges list <id>` | List judges with progress |
| `hackathon judging judges scope <id> <pid>` | Inspect or change a judge's prizes and rooms |
| `hackathon judging judges add <id>` | Add a judge |
| `hackathon judging judges remove <id> <pid>` | Remove a judge |
| `hackathon judging assignments list <id>` | List judge assignments |
| `hackathon judging assignments create <id>` | Create an assignment |
| `hackathon judging assignments delete <id> <aid>` | Delete an assignment |
| `hackathon judging auto-assign <id>` | Legacy per-judge cap, or assignment preview |
| `hackathon judging invitations list <id>` | List pending invitations |
| `hackathon judging invitations cancel <id> <iid>` | Cancel an invitation |
| `hackathon judging pick-results <id>` | View subjective judging tallies |

See [the judging CLI guide](/docs/guides/cli-judging) for setup and retry examples. The retired `judging levels` aliases explain the supported scorecard commands; they do not call an unavailable rubric API.

### Prizes

| Command | Description |
|---------|-------------|
| `hackathon prizes list <id>` | List prizes |
| `hackathon prizes create <id>` | Create a prize |
| `hackathon prizes update <id> <pid>` | Update a prize |
| `hackathon prizes delete <id> <pid>` | Delete a prize |
| `hackathon prizes reorder <id>` | Reorder prizes |
| `hackathon prizes assign <id> <pid>` | Assign prize to submission |
| `hackathon prizes unassign <id> <pid> <sid>` | Unassign prize from submission |

### Judge Display

| Command | Description |
|---------|-------------|
| `hackathon judge-display list <id>` | List display profiles |
| `hackathon judge-display create <id>` | Create a display profile |
| `hackathon judge-display update <id> <jid>` | Update a display profile |
| `hackathon judge-display delete <id> <jid>` | Delete a display profile |
| `hackathon judge-display reorder <id>` | Reorder display profiles |

### Results

| Command | Description |
|---------|-------------|
| `hackathon results calculate <id>` | Calculate rankings from scores |
| `hackathon results get <id>` | View detailed results |
| `hackathon results publish <id>` | Publish results |
| `hackathon results unpublish <id>` | Unpublish results |

### Webhooks

| Command | Description |
|---------|-------------|
| `hackathon webhooks list` | List webhooks |
| `hackathon webhooks create` | Create a webhook |
| `hackathon webhooks delete <id>` | Delete a webhook |

### Jobs

| Command | Description |
|---------|-------------|
| `hackathon jobs list` | List jobs |
| `hackathon jobs get <id>` | Get job details |
| `hackathon jobs create` | Create a job |
| `hackathon jobs result <id>` | Get job result |
| `hackathon jobs cancel <id>` | Cancel a job |

### Schedules

| Command | Description |
|---------|-------------|
| `hackathon schedules list` | List schedules |
| `hackathon schedules create` | Create a schedule |
| `hackathon schedules get <id>` | Get schedule details |
| `hackathon schedules update <id>` | Update a schedule |
| `hackathon schedules delete <id>` | Delete a schedule |

<Callout type="info">
Use `hackathon <command> --help` for full flag details on any command.
</Callout>

## Next Steps

- [Getting Started](/docs/cli/getting-started) — Authentication and your first commands
- [Organizer Guide](/docs/guides/cli-organizer) — Run a hackathon end-to-end from the CLI
- [Browsing Guide](/docs/guides/cli-browsing) — Discover hackathons and view results
- [Judging Guide](/docs/guides/cli-judging) — Manage judges, criteria, and assignments
- [CI/CD Guide](/docs/guides/cli-automation) — Automate workflows with the CLI
